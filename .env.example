# Production database URL (Postgres)
# In development, this is not needed — the database package falls back to
# packages/database/prisma/dev.db automatically.
# DATABASE_URL=postgresql://user:password@host:5432/dbname

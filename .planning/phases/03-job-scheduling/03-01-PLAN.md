---
phase: 03-job-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ingestion/package.json
  - apps/ingestion/src/scheduler.ts
  - apps/ingestion/src/jobs/index.ts
  - apps/ingestion/src/main.ts
autonomous: true

must_haves:
  truths:
    - 'Bree scheduler starts without errors when ingestion app starts'
    - "Scheduler remains active and doesn't crash during operation"
    - 'Job files can be registered with Bree without path resolution errors'
  artifacts:
    - path: 'apps/ingestion/src/scheduler.ts'
      provides: 'Bree scheduler instance and initialization logic'
      min_lines: 30
    - path: 'apps/ingestion/src/jobs/index.ts'
      provides: 'Job registry exporting job metadata for scheduler'
      min_lines: 15
    - path: 'apps/ingestion/package.json'
      provides: '@breejs/bree dependency for scheduling'
      contains: '@breejs/bree'
  key_links:
    - from: 'apps/ingestion/src/main.ts'
      to: 'apps/ingestion/src/scheduler.ts'
      via: 'import and initialize scheduler'
      pattern: 'scheduler.*start'
    - from: 'apps/ingestion/src/scheduler.ts'
      to: 'apps/ingestion/src/jobs'
      via: 'register job files'
      pattern: 'new Bree'
---

## Objective

Install and configure Bree job scheduler as the foundation for all automated
scraper jobs. Create scheduler instance that will start on app launch and
register job files.

**Purpose**: Establish the scheduling infrastructure that will execute daily
scraper jobs with worker thread isolation.

**Output**:

- `@breejs/bree` added as dependency
- `apps/ingestion/src/scheduler.ts` with Bree instance configuration
- `apps/ingestion/src/jobs/index.ts` with job metadata registry
- Main process initializes scheduler on startup

## Execution Context

@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md
@.planning/phases/03-job-scheduling/CONTEXT.md

Reference: Phase 2 complete with API layer running on Fastify See:
apps/ingestion/src/main.ts (current main scraper entry point)

## Tasks

<task type="auto">
  <name>Task 1: Install @breejs/bree and configure scheduler instance</name>
  <files>apps/ingestion/package.json, apps/ingestion/src/scheduler.ts</files>
  <action>
    1. Add "@breejs/bree" to apps/ingestion/package.json dependencies (latest stable version from npm)
    2. Create apps/ingestion/src/scheduler.ts with Bree scheduler instance
    3. Scheduler config:
       - Create new Bree instance
       - Set root: path.join(import.meta.url, '../jobs') for jobs directory (use import.meta.url to avoid path issues)
       - Enable worker thread reuse (pool workers by default in Bree)
       - Add graceful shutdown handler (catch SIGTERM, call scheduler.stop())
       - Export scheduler instance for use in main.ts
    4. Add helpful comments explaining Bree worker thread model and job registration pattern
    5. Do NOT register any jobs in this task (that's task 2)
  </action>
  <verify>
    npm ls @breejs/bree in apps/ingestion/ shows the package is installed
    TypeScript compilation of scheduler.ts has no errors
    Scheduler instance can be imported without runtime errors
  </verify>
  <done>
    - @breejs/bree added to package.json
    - scheduler.ts exports Bree instance configured with jobs directory
    - Scheduler has graceful shutdown handler registered
    - No TypeScript errors, ready for job registration
  </done>
</task>

<task type="auto">
  <name>Task 2: Create job metadata registry and update main.ts to start scheduler</name>
  <files>apps/ingestion/src/jobs/index.ts, apps/ingestion/src/main.ts</files>
  <action>
    1. Create apps/ingestion/src/jobs/index.ts with job registry:
       - Export const JOBS array with job metadata objects
       - Each job object: { name: string, path: string (relative to jobs dir), enabled: boolean }
       - Include placeholder entries for deputies and voting jobs (will be created in Plan 2):
         * { name: 'deputies', path: './deputies.ts', enabled: false }
         * { name: 'voting', path: './voting.ts', enabled: false }
       - Export MAX_CONCURRENT_JOBS constant: 1 (run one job at a time to avoid db contention)
    2. Update apps/ingestion/src/main.ts:
       - Add conditional: if process.argv contains '--no-scheduler' flag, skip scheduler startup (for manual scrapes)
       - Default behavior (no flag): Initialize scheduler and register jobs from JOBS array
       - Call scheduler.start() after initialization
       - Add console log: "Scheduler started, waiting for jobs..."
       - Keep existing scraper logic unchanged (still runs on CLI flags like --source=)
    3. Ensure main() function waits for scheduler without blocking (use setInterval to keep process alive, or let Bree keep process alive)
  </action>
  <verify>
    npm run scrape (with no args) starts scheduler instead of running scrapers
    npm run scrape --no-scheduler runs manual scrape as before
    "Scheduler started, waiting for jobs..." appears in logs
    Process remains running (doesn't exit after initialization)
  </verify>
  <done>
    - apps/ingestion/src/jobs/index.ts exports JOBS array with all planned jobs
    - main.ts conditionally starts scheduler or runs manual scrapes
    - Scheduler initialization logs confirmation message
    - Process stays alive when scheduler is running
  </done>
</task>

</tasks>

## Verification

After this plan completes:

1. **Scheduler Installation**: `pnpm install` in apps/ingestion/ succeeds,
   @breejs/bree appears in node_modules
2. **No Startup Errors**: `npm run scrape` starts scheduler without crashing
   (Bree instance created successfully)
3. **Log Output**: "Scheduler started, waiting for jobs..." appears in console
   within 2 seconds of startup
4. **Graceful Shutdown**: `Ctrl+C` stops the process cleanly (SIGTERM handler
   called)
5. **Manual Scrape Still Works**:
   `npm run scrape --source=deputies --no-scheduler` runs deputies scraper
   directly (bypassing scheduler)
6. **TypeScript**: `npm run lint:types` passes with no errors in scheduler.ts
   and main.ts

## Success Criteria

- [ ] Bree scheduler instance created and exported from scheduler.ts
- [ ] Scheduler initializes on app startup (no --no-scheduler flag)
- [ ] Scheduler remains active (process doesn't exit)
- [ ] Job registry in place with deputies and voting entries (disabled
      initially)
- [ ] Graceful shutdown works cleanly
- [ ] Manual scrape mode bypasses scheduler (backwards compatibility)
- [ ] All TypeScript types valid
- [ ] No runtime errors during startup

## Output

After completion, create `.planning/phases/03-job-scheduling/03-01-SUMMARY.md`
